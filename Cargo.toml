[package]
name = "hyperfund"
version = "0.1.0"
edition = "2021"
description = "Real-time Hyperliquid funding rate monitor and delta-neutral execution engine"
license = "MIT"

[[bin]]
name = "hyperfund"
path = "src/main.rs"

[dependencies]
tokio = { version = "1", features = ["full"] }
reqwest = { version = "0.12", features = ["json"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
colored = "2"
clap = { version = "4", features = ["derive"] }
anyhow = "1"
